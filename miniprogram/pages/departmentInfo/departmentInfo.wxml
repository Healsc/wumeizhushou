<cu-custom bgColor="bg-gradual-blue" isBack="{{true}}">
	<view slot="backText">返回</view>
	<view slot="content">{{department._name}}</view>
</cu-custom>
<view class="detail">  
	<!-- <htmltowxml text="{{content}}" /> -->
	<wxparser rich-text="{{content}}" />
</view>