<cu-custom bgColor="bg-gradual-pink" isBack="{{true}}">
	<view slot="backText">返回</view>
	<view slot="content">活动安排</view>
</cu-custom>
<view class="detail">  
	<!-- <htmltowxml text="{{content}}" /> -->
	<wxparser rich-text="{{activeDetail._content}}" />
</view>